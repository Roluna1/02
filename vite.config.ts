import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist', // Ensure this matches your deployment setup
    rollupOptions: {
      // Ensure proper configuration here if necessary
    }
  }
});
